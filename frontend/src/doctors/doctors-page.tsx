import { useQuery } from "@tanstack/react-query"; import { api } from "../lib/api"; import type { Doctor } from "../types/api"; import { PageHeader } from "../components/page-header"; import { Card } from "../components/ui/card"; import { EmptyState, ErrorState, LoadingState } from "../components/feedback";
export function DoctorsPage(){const query=useQuery({queryKey:["doctors"],queryFn:()=>api<Doctor[]>("/api/doctors")});return <><PageHeader title="Врачи" description="Команда клиники и статус учётных записей"/>{query.isLoading?<LoadingState/>:query.error?<ErrorState error={query.error}/>:!query.data?.length?<EmptyState title="Врачей нет" text="Добавьте первую учётную запись врача через API"/>:<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{query.data.map(doctor=><Card className="p-5" key={doctor.id}><div className="flex items-start justify-between gap-3"><div><h2 className="font-semibold text-slate-900">{doctor.fullName}</h2><p className="mt-1 text-sm text-slate-500">{doctor.specialization||"Специализация не указана"}</p></div><span className={doctor.active?"rounded-full bg-emerald-50 px-2 py-1 text-xs font-medium text-emerald-700":"rounded-full bg-slate-100 px-2 py-1 text-xs text-slate-600"}>{doctor.active?"Активен":"Неактивен"}</span></div><div className="mt-5 space-y-1 text-sm text-slate-600"><p>{doctor.phone||"Телефон не указан"}</p><p className="text-slate-400">@{doctor.username}</p></div></Card>)}</div>}</>}
