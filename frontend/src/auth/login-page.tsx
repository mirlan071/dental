import { useState,type FormEvent } from "react"; import { Navigate, useLocation, useNavigate } from "react-router-dom"; import { Stethoscope } from "lucide-react"; import { useAuth } from "./auth-context"; import { Button } from "../components/ui/button"; import { Input } from "../components/ui/input"; import { LoadingState } from "../components/feedback"; import { errorMessage } from "../lib/api";
export function LoginPage(){const{user,loading,login}=useAuth();const navigate=useNavigate();const location=useLocation();const[username,setUsername]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[pending,setPending]=useState(false);if(loading)return <LoadingState label="Проверяем сессию…"/>;if(user)return <Navigate to={user.role==="ADMIN"?"/dashboard":"/my-day"} replace/>;
 async function submit(e:FormEvent){e.preventDefault();setPending(true);setError("");try{const current=await login(username,password);const destination=(location.state as {from?:{pathname:string}}|null)?.from?.pathname;navigate(destination??(current.role==="ADMIN"?"/dashboard":"/my-day"),{replace:true})}catch(err){setError(errorMessage(err))}finally{setPending(false)}}
 return <main className="grid min-h-screen place-items-center bg-slate-100 px-4"><div className="w-full max-w-sm"><div className="mb-7 flex items-center justify-center gap-3"><span className="grid size-11 place-items-center rounded-xl bg-brand-700 text-white"><Stethoscope size={23}/></span><div><p className="text-xl font-semibold text-slate-950">Dental CRM</p><p className="text-xs text-slate-500">Управление клиникой</p></div></div><form onSubmit={submit} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h1 className="text-xl font-semibold text-slate-950">Вход в систему</h1><p className="mt-1 text-sm text-slate-500">Используйте учётную запись клиники</p><label className="mt-6 block text-sm font-medium text-slate-700">Имя пользователя<Input className="mt-2" autoComplete="username" value={username} onChange={e=>setUsername(e.target.value)} required autoFocus/></label><label className="mt-4 block text-sm font-medium text-slate-700">Пароль<Input className="mt-2" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p role="alert" className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<Button className="mt-6 w-full" disabled={pending}>{pending?"Входим…":"Войти"}</Button></form></div></main>}
